// for in loop 

const fruits = ["Apple", "Orange", "Peru", "Kiwi", "Chiku"];

for (let fruit in fruits) {
    console.log(fruit);                    // this will give the index length of the array
    console.log(fruits[fruit]);            // this will print the array item
}

// console.log(fruits[fruit]);

for (let frui in fruits) {
    console.log(frui);
}

for (let fru in fruits) {
    console.log(fruits[fru]);
}